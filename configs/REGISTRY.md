# Noise-model registry

*Generated by `scripts/export_registry.py` from `eta_pipeline/models.py`. Do not edit by hand; `tests/test_registry.py` fails if this file and the code disagree.*

Maps are 128x128 pixels with a Gaussian beam of FWHM 5.0 px. Spectra use the unnormalized FFT: E|fft2(n)|^2 = P(k), k in cycles/pixel convention. Ensembles are split into PSD / FIT / VAL / EVAL with fractions 0.25 / 0.4 / 0.1 / 0.25 and split seed 12345 (default ensemble size 6000).

Image *i* of a model is drawn from child *i* of `numpy.random.SeedSequence(master_seed)`, so any single map can be regenerated with `eta_pipeline.registry_tools.reconstruct_map(model, i)`. White floors of the `*_WN` rows come from an independent stream at master seed + 606000.

Notes on the columns. *tier* is the code's label (the prefix of the code name); the paper shows that PCA leakage is a Gaussian scale mixture and discusses it with the Tier-1 models (Sec. 7), while the code name `T2_PCA` is kept so that result files stay addressable. *default rungs* is the ladder a bare `run_eta_campaign.py` run uses (`cnn` is the paper's "conv rung"); the production cells added the `reweight` rung and other settings per cell -- those are recorded in `scripts/hpc/cells*.txt` and, authoritatively, in the `variational2.config` block of every result file in `results/eta_results/`.

| code name | paper name | tier | master seed | white floor sigma_w | default rungs | exact quadrature | complete-data bound |
|---|---|---|---|---|---|---|---|
| `T0_WHITE` | white noise | T0 | 777000 | 0 | linear, quadratic, cnn | - | - |
| `T0_RED` | red (1/f^3) noise | T0 | 777017 | 0 | linear, quadratic, cnn | - | - |
| `T0_RED_REAL` | red + white noise | T0 | 777034 | 1 | linear, quadratic, cnn | - | - |
| `T0_ANISO` | fixed-direction anisotropy | T0 | 777051 | 0 | linear, quadratic, cnn | - | - |
| `T1_PSRAND` | spectral-tilt mixture | T1 | 777068 | 0 | linear, quadratic, cnn | yes | - |
| `T1_PSRAND_SLOPE` | slope-only tilt mixture | T1 | 777085 | 0 | (cheap only) | yes | - |
| `T1_PSRAND_AMP` | amplitude-only mixture | T1 | 777102 | 0 | (cheap only) | yes | - |
| `T1_RANDOR` | random-orientation anisotropy | T1 | 777119 | 0 | linear, quadratic, cnn | yes | - |
| `T2_MEDIAN` | row-median residual | T2 | 777136 | 0 | linear, quadratic, cubic, cnn | - | - |
| `T2_CROSS_SYM` | scan crossings (sym.) | T2 | 777153 | 0 | linear, quadratic, cubic, cnn | - | yes |
| `T2_CROSS_POS` | scan crossings (pos.) | T2 | 777170 | 0 | linear, quadratic, cubic, cnn | - | yes |
| `T2_GLITCH` | sub-threshold glitches | T2 | 777187 | 0 | linear, quadratic, cubic, cnn | - | yes |
| `T2_PCA` | PCA leakage | T2 | 777204 | 0 | linear, quadratic, cubic, cnn | - | yes |
| `T1_PSRAND_WN` | spectral-tilt mixture + white floor | T1 | 777221 | 0.27 | linear, quadratic, cnn | yes | - |
| `T1_PSRAND_SLOPE_WN` | slope-only tilt mixture + white floor | T1 | 777238 | 0.27 | (cheap only) | yes | - |
| `T1_RANDOR_WN` | random-orientation anisotropy + white floor | T1 | 777255 | 1.04 | linear, quadratic, cnn | yes | - |
| `T2_MEDIAN_WN` | row-median residual + white floor | T2 | 777272 | 1.06 | linear, quadratic, cubic, cnn | - | - |
| `T2_CROSS_SYM_WN` | scan crossings (sym.) + white floor | T2 | 777289 | 0.51 | linear, quadratic, cubic, cnn | - | yes |
| `T2_CROSS_POS_WN` | scan crossings (pos.) + white floor | T2 | 777306 | 0.48 | linear, quadratic, cubic, cnn | - | yes |
| `T2_GLITCH_WN` | sub-threshold glitches + white floor | T2 | 777323 | 0.86 | linear, quadratic, cubic, cnn | - | yes |
| `T2_PCA_WN` | PCA leakage + white floor | T2 | 777340 | 0.1 | linear, quadratic, cubic, cnn | - | yes |
| `T2_CONFUSION` | source confusion | T2 | 777357 | 0 | linear, quadratic, cubic, cnn | - | - |
| `T2_CONFUSION_WN` | source confusion + white floor | T2 | 777374 | 3.22441 | linear, quadratic, cubic, cnn | - | yes |
| `T2_CONFUSION_ATM` | beam-sharing companion | T2 | 777391 | 0 | linear, quadratic, cubic, cnn | - | yes |

## Seed streams

| stream | seed |
|---|---|
| white_floor | model seed + 606000 |
| components | model seed + 991 |
| teaser_train | model seed + 103000 |
| teaser_eval | model seed + 213000 |
| high_precision_quadrature | 777000 + 555000 + (row index in run_b2b_cheap.py) |
| closure_tests | 777000 + 900001 + int(1000 * sigma_alpha)   (run_b2b_cheap.py) |

## Severity parameters per model

### `T0_WHITE`

Generator `gen_t0_white`. white Gaussian benchmark; production η=1 null

```json
{
  "white_sigma": 1.0
}
```

### `T0_RED`

Generator `gen_t0_red`. isotropic 1/f³ + beam; production η=1 null

```json
{
  "psd_isotropic": {
    "slope": 3.0,
    "amplitude": 5.0
  },
  "beam_fwhm_pix": 5.0
}
```

### `T0_RED_REAL`

Generator `gen_t0_red_real`. realistic: beamed 1/f³ + unsmoothed white; η=1 null

```json
{
  "psd_isotropic": {
    "slope": 3.0,
    "amplitude": 5.0
  },
  "beam_fwhm_pix": 5.0,
  "white_sigma_unsmoothed": 1.0
}
```

### `T0_ANISO`

Generator `gen_t0_aniso`. fixed-direction anisotropic Gaussian; η=1 with 2-D-PSD whitening

```json
{
  "psd_two_component": {
    "slope_scan": 3.0,
    "A_scan": 0.5,
    "slope_iso": 3.0,
    "A_iso": 5.0
  },
  "beam_fwhm_pix": 5.0
}
```

### `T1_PSRAND`

Generator `gen_t1_psrand`. per-image slope+amplitude randomization (flagship §5.1); quadrature exact; cumulants unreliable (variance-mixture tails)

```json
{
  "psrand": {
    "slope_mean": 3.0,
    "slope_sigma": 0.5,
    "amp_mean": 5.0,
    "amp_sigma": 0.8
  },
  "beam_fwhm_pix": 5.0
}
```

### `T1_PSRAND_SLOPE`

Generator `gen_t1_psrand_slope`. slope-only variant (cheap-only): quadrature closure vs the M4 §5.2 dispersion scaling

```json
{
  "psrand": {
    "slope_mean": 3.0,
    "slope_sigma": 0.5,
    "amp_mean": 5.0,
    "amp_sigma": 0.0
  },
  "beam_fwhm_pix": 5.0
}
```

### `T1_PSRAND_AMP`

Generator `gen_t1_psrand_amp`. amplitude-only variant (cheap-only): η_marg = E[σ²]E[σ⁻²] closure (erratum 1)

```json
{
  "psrand": {
    "slope_mean": 3.0,
    "slope_sigma": 0.0,
    "amp_mean": 5.0,
    "amp_sigma": 0.8
  },
  "beam_fwhm_pix": 5.0
}
```

### `T1_RANDOR`

Generator `gen_t1_randor`. random-orientation anisotropy (§5.2); quadrature exact over θ

```json
{
  "psd_two_component": {
    "slope_scan": 3.0,
    "A_scan": 0.5,
    "slope_iso": 3.0,
    "A_iso": 5.0
  },
  "scan_angle": "uniform on [0, pi)",
  "beam_fwhm_pix": 5.0
}
```

### `T2_MEDIAN`

Generator `gen_t2_median`. fixed anisotropy + row-median removal; light tails, cumulants OK; no complete-data bound (structure is processing-induced, not additive)

```json
{
  "psd_two_component": {
    "slope_scan": 3.0,
    "A_scan": 0.5,
    "slope_iso": 3.0,
    "A_iso": 5.0
  },
  "beam_fwhm_pix": 5.0,
  "processing": "row-median removal (axis=1)"
}
```

### `T2_CROSS_SYM`

Generator `gen_t2_cross_sym`. scan crossings, sign-symmetric (trispectrum-led; quadratic-rung null expected §21.2); t_2.5 marks ⇒ cumulants unreliable

```json
{
  "psd_isotropic": {
    "slope": 3.0,
    "amplitude": 5.0
  },
  "beam_fwhm_pix": 5.0,
  "scan_crossings": {
    "n_crossings_mean": 8,
    "along_scan_corr": 30.0,
    "beam_sigma_rows": 2.0,
    "amp_scale": 0.5,
    "amp_df": 2.5
  },
  "sign_convention": "symmetric"
}
```

### `T2_CROSS_POS`

Generator `gen_t2_cross_pos`. scan crossings, positive-definite (bispectrum-led legacy variant)

```json
{
  "psd_isotropic": {
    "slope": 3.0,
    "amplitude": 5.0
  },
  "beam_fwhm_pix": 5.0,
  "scan_crossings": {
    "n_crossings_mean": 8,
    "along_scan_corr": 30.0,
    "beam_sigma_rows": 2.0,
    "amp_scale": 0.5,
    "amp_df": 2.5
  },
  "sign_convention": "positive"
}
```

### `T2_GLITCH`

Generator `gen_t2_glitch`. sub-threshold glitches (Pareto a=2.5 marks: finite 4th moment, cumulants heavy but defined); Campbell analytics in B2c

```json
{
  "psd_two_component": {
    "slope_scan": 3.0,
    "A_scan": 0.5,
    "slope_iso": 3.0,
    "A_iso": 5.0
  },
  "beam_fwhm_pix": 5.0,
  "glitches": {
    "n_glitches": 25,
    "tau_pix": 5.0,
    "amp_scale": 0.3,
    "sigma_beam": 2.0,
    "sigma_cross": 2.0
  }
}
```

### `T2_PCA`

Generator `gen_t2_pca`. PCA leaked modes (t_2.5 marks ⇒ cumulants unreliable); tier factorization surrogate in B2c

```json
{
  "psd_isotropic": {
    "slope": 3.0,
    "amplitude": 5.0
  },
  "beam_fwhm_pix": 5.0,
  "pca_leakage": {
    "n_leaked_mean": 3,
    "leak_efficiency": 0.15,
    "A_iso": 5.0,
    "leak_df": 2.5,
    "mode_corr_scan": 20.0,
    "mode_corr_cross": 40.0
  }
}
```

### `T1_PSRAND_WN`

Generator `gen_t1_psrand_wn`. PSRAND + unsmoothed white floor: the band-regularized flagship. Quadrature is the non-factorized 2-D form (the floor breaks P = a^2 P_base).  No AMP-only twin exists by design -- see note on T1_PSRAND_AMP.

```json
{
  "psrand": {
    "slope_mean": 3.0,
    "slope_sigma": 0.5,
    "amp_mean": 5.0,
    "amp_sigma": 0.8
  },
  "beam_fwhm_pix": 5.0
}
```

### `T1_PSRAND_SLOPE_WN`

Generator `gen_t1_psrand_slope_wn`. slope-only floored variant (cheap-only): the closure test against the EFFECTIVE lever arm u_eff = (P_red/P_tot) log(k/k_piv), which only applies once a non-tilting component exists

```json
{
  "psrand": {
    "slope_mean": 3.0,
    "slope_sigma": 0.5,
    "amp_mean": 5.0,
    "amp_sigma": 0.0
  },
  "beam_fwhm_pix": 5.0
}
```

### `T1_RANDOR_WN`

Generator `gen_t1_randor_wn`. random-orientation anisotropy + white floor; the incoherent-channel exemplar with a regularized compact band

```json
{
  "psd_two_component": {
    "slope_scan": 3.0,
    "A_scan": 0.5,
    "slope_iso": 3.0,
    "A_iso": 5.0
  },
  "scan_angle": "uniform on [0, pi)",
  "beam_fwhm_pix": 5.0
}
```

### `T2_MEDIAN_WN`

Generator `gen_t2_median_wn`. row-median removal + white floor.  NOTE: the floor is applied BEFORE the median (Phase-A pipeline order); a row median is nonlinear, so this is not the same model as median-then-floor.  No complete-data bound (structure is processing-induced, not additive)

```json
{
  "psd_two_component": {
    "slope_scan": 3.0,
    "A_scan": 0.5,
    "slope_iso": 3.0,
    "A_iso": 5.0
  },
  "beam_fwhm_pix": 5.0,
  "processing": "row-median removal (axis=1)"
}
```

### `T2_CROSS_SYM_WN`

Generator `gen_t2_cross_sym_wn`. scan crossings (sign-symmetric) + white floor

```json
{
  "psd_isotropic": {
    "slope": 3.0,
    "amplitude": 5.0
  },
  "beam_fwhm_pix": 5.0,
  "scan_crossings": {
    "n_crossings_mean": 8,
    "along_scan_corr": 30.0,
    "beam_sigma_rows": 2.0,
    "amp_scale": 0.5,
    "amp_df": 2.5
  },
  "sign_convention": "symmetric"
}
```

### `T2_CROSS_POS_WN`

Generator `gen_t2_cross_pos_wn`. scan crossings (positive-definite) + white floor

```json
{
  "psd_isotropic": {
    "slope": 3.0,
    "amplitude": 5.0
  },
  "beam_fwhm_pix": 5.0,
  "scan_crossings": {
    "n_crossings_mean": 8,
    "along_scan_corr": 30.0,
    "beam_sigma_rows": 2.0,
    "amp_scale": 0.5,
    "amp_df": 2.5
  },
  "sign_convention": "positive"
}
```

### `T2_GLITCH_WN`

Generator `gen_t2_glitch_wn`. sub-threshold glitches + white floor.  The floor also lowers the per-event significance xi, which README_PhaseB2a Sec. 5 traced to its absence -- recompute f_s and xi_rms before touching amp_scale

```json
{
  "psd_two_component": {
    "slope_scan": 3.0,
    "A_scan": 0.5,
    "slope_iso": 3.0,
    "A_iso": 5.0
  },
  "beam_fwhm_pix": 5.0,
  "glitches": {
    "n_glitches": 25,
    "tau_pix": 5.0,
    "amp_scale": 0.3,
    "sigma_beam": 2.0,
    "sigma_cross": 2.0
  }
}
```

### `T2_PCA_WN`

Generator `gen_t2_pca_wn`. PCA leaked modes + white floor; the flagship Tier-2 case

```json
{
  "psd_isotropic": {
    "slope": 3.0,
    "amplitude": 5.0
  },
  "beam_fwhm_pix": 5.0,
  "pca_leakage": {
    "n_leaked_mean": 3,
    "leak_efficiency": 0.15,
    "A_iso": 5.0,
    "leak_df": 2.5,
    "mode_corr_scan": 20.0,
    "mode_corr_cross": 40.0
  }
}
```

### `T2_CONFUSION`

Generator `gen_t2_confusion`. confusion noise, unclustered Poisson, NO floor.  The compact template has |tau~|^2/P exactly flat, so the MF has zero spectral leverage -- but flat is not convergent: the Fisher sum is mode-count-limited and ends up decided by floating point, not by physics.  Kept as the diagnostic that establishes this; the quotable numbers come from T2_CONFUSION_WN.  cumulant_ok is False, but NOT for the usual reason: the marks are perfectly well behaved (a dN/dS truncated at S_cut has finite q_3 and q_4 by construction, unlike the t_2.5 marks of CROSS/PCA).  Method B fails here because the field is nowhere near weakly non-Gaussian -- pixel skewness 2.13, excess kurtosis 9.14, and the measured ||k4(t^)||^2 is O(10^4) where the perturbative expansion needs <<1 -- and because the estimator subtracts O(M) disconnected pieces at M = 16384 (memo v2 Sec. 17).  Measured eta_pert came out -12052 +/- 8719: consistent with anything.

```json
{
  "confusion": {
    "counts": "Schechter fit to 350 um counts (noise_lib.confusion)",
    "s_min_mJy": 0.1,
    "s_cut_mJy": 100.0,
    "sigma_c_mJy_per_beam": 6.4488194699641825,
    "pixel_arcsec": 4.0,
    "beam_fwhm_arcsec": 20.0
  }
}
```

### `T2_CONFUSION_WN`

Generator `gen_t2_confusion_wn`. confusion + unsmoothed white floor at f_N = 0.5 (the production row).  Stationary, additive, no Tier-1 confound, both templates band-interior, bound stable: the best-conditioned Tier-2 case in the taxonomy.  Complete-data bounds 41.7 (compact) / 201.9 (extended) -- by far the largest in the campaign, and they are the formal ceiling for perfect cataloguing of every source to S_min.  cumulant_ok False for the same reason as the floorless row: the marks are fine, the field is simply far outside the perturbative regime and Method B is variance-limited at this pixel count.

```json
{
  "confusion": {
    "counts": "Schechter fit to 350 um counts (noise_lib.confusion)",
    "s_min_mJy": 0.1,
    "s_cut_mJy": 100.0,
    "sigma_c_mJy_per_beam": 6.4488194699641825,
    "pixel_arcsec": 4.0,
    "beam_fwhm_arcsec": 20.0,
    "f_N": 0.5
  }
}
```

### `T2_CONFUSION_ATM`

Generator `_gen_confusion_atm`. confusion + a BEAM-CORRELATED Gaussian companion at rho = 0.25 (the same 0.5 sigma_c of Gaussian noise as T2_CONFUSION_WN, differing only in whether it shares the beam).  The exactly solvable reference: whitening returns i.i.d. pixels, so eta is template-independent and equal to Var[Y] I(Y) for Y = D + W, = 3.569 at this configuration, with a complete-data bound of exactly 5.  A NULL TEST, not a production row: the band is not regularised (P is still ~|B|^2), and the gap between the measured and exact eta measures what the rounding side-channel costs.  floor = 0.0 is correct -- the companion is not an unsmoothed floor and must not be reported as one.

```json
{
  "confusion": {
    "counts": "Schechter fit to 350 um counts (noise_lib.confusion)",
    "s_min_mJy": 0.1,
    "s_cut_mJy": 100.0,
    "sigma_c_mJy_per_beam": 6.4488194699641825,
    "pixel_arcsec": 4.0,
    "beam_fwhm_arcsec": 20.0,
    "beam_correlated_companion_rho": 0.25
  }
}
```

